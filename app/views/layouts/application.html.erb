<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "TODOMOTO | your todos" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="mobile-web-app-capable" content="yes">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">

    <link rel="icon" href="/todomoto_favicon.svg" type="image/svg+xml">
    <link rel="icon" href="/todomoto_favicon.ico" type="image/ico">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link rel="preconnect" href="https://fonts.googleapis.com" crossorigin />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <!-- preload stylesheet -->
    <link rel="preload" href="https://fonts.googleapis.com/icon?family=Material+Icons" as="style" crossorigin>
    <!-- preload font that is in stylesheeet above-->
    <link rel="preload" href="https://fonts.gstatic.com/s/materialicons/v142/flUhRq6tzZclQEJ-Vdg-IuiaDsNc.woff2"
                        as="font" type="font/woff2" crossorigin>
    <!-- load icons as stylesheet -->
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">


  </head>

  <body>

  <%= render partial: "shared/layout/header" %>
    <main class="container">
      <%= yield %>
    </main>
  </body>
</html>
